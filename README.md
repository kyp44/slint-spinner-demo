# Slint `Spinner` Bug Demo

This is just a demonstration for a possible bug with rending the `Spinner` component in Slint.

The bug report can be found [here](https://github.com/slint-ui/slint/issues/5237).
